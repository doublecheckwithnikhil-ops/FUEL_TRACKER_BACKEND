BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[FuelEntry] ADD [createdAt] DATETIME2 CONSTRAINT [FuelEntry_createdAt_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
